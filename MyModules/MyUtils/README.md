# MyUtils

A miscellany of utility functions.

## Why

These are functions which I house here until I find a better place for them. They are automatically loaded using PowerShell's `$env:PSModulePath`.

## What's in it

- `Select-OmpTheme` - Uses fzf to select an OhMyPosh theme from the list of installed themes, then changes to that theme.
